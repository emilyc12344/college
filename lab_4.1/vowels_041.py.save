#!/usr/bin/env python3

import sys

lines = []
letters = {}

for l in sys.stdin:
    l = ''.join(char for char in l if char.isalnum() or char == "'")
print(l)

for char in l:
    if char in letters:
        

