#!/usr/bin/env python3

import sys

lines = sys.stdin.readlines()

com = sorted(lines[0].strip().split())
lines = lines[1:]

for curr in lines:
    curr = curr.strip().split()
    for ani in com:
        if ani not in curr:
            com.pop(com.index(ani))

print(len(com))
for i in com:
    print(i)
